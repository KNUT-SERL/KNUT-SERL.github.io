# 빠른 요약 (자세한 내용은 README.md)

모든 관리 방법은 **README.md** 한 파일에 있습니다. 아래는 자주 하는 일 요약입니다.

| 하고 싶은 일 | 방법 |
|---|---|
| 구성원 추가 | `members/`에 `MS-006-GildongHong.txt`(프로필) + 같은 이름 사진 업로드 |
| 구성원 사진 교체 | `members/`에 같은 이름으로 다시 업로드 |
| 졸업 처리 | 그 사람 `.txt` 이름을 `ALU-순번-이름.txt`로 바꾸고 내용을 졸업생 형식으로 |
| 연구 분야 | `research/`에 `R-001-이름.jpg` + 같은 이름 `.txt`(제목·요약) 업로드 |
| 수상 상장 | `awards/`에 `A-2024-001-상장 이름.png` 업로드 → 뉴스 연도 칸 맨 위에 가로 나열 (이름 수정 = 파일명 변경) |
| 갤러리 사진 | `gallery/G-2025-001-행사이름/` 폴더에 `제목.txt`(제목·영문) + 사진 `01.jpg 02.jpg…` 업로드 (README 2장) |
| 뉴스 추가 | `data/news.txt` 맨 아래에 한 묶음 추가 — 사진은 `images/news/`에 올리고 `사진:`에 경로 (README 3장) |
| 뉴스 펼침·링크 | `본문:`·`사진:`을 채우면 카드가 클릭으로 펼쳐짐, `링크:`를 채우면 LINK 배지 |
| 뉴스 삭제 | `날짜:`부터 `사진:`까지 묶음 통째로 삭제 |
| 특허 | `data/patents.txt` 맨 아래에 한 묶음 추가 (번호 하나 올려서) |
| 논문 | 자동 — 할 일 없음 |
| 사이트 검색 | 자동 — 메뉴 🔍 또는 Ctrl+K 로 열고 × 또는 Esc 로 닫기 (파일을 올리면 검색에도 바로 반영) |
| 로고 교체 | 상단 `images/logo.png` · 푸터 학교 로고 `images/knut-logo-white.png`(흰색 버전) · 탭 아이콘 `images/favicon-32.png` 등을 같은 이름으로 덮어쓰기 (README 11장) |
| 반영이 안 될 때 | 1~2분 후 Ctrl+F5 → 그래도 안 되면 README 13장 |

구성원 접두사: `PHD` 포닥 · `DR` 박사 · `DRMS` 석박사연계 · `MS` 석사 · `MSBS` 학석사연계 · `BS` 학사 · `INT` 인턴 · `ALU` 졸업생
